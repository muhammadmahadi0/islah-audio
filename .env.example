# Piped API Instance (can be swapped easily)
NEXT_PUBLIC_PIPED_API=https://pipedapi.kavin.rocks

# Fallback instances (comma-separated)
NEXT_PUBLIC_PIPED_FALLBACK=

# Channel to fetch videos from (YouTube handle)
NEXT_PUBLIC_CHANNEL_HANDLE=@islahbd