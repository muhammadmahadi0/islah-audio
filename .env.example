# YouTube Data API v3 key (required for channel listing + metadata).
# Get one at https://console.cloud.google.com/apis/library/youtube.googleapis.com
# Must also be set in the Netlify dashboard (Site settings > Environment variables).
YOUTUBE_API_KEY=

# Channel to fetch videos from (YouTube channel ID)
NEXT_PUBLIC_CHANNEL_HANDLE=@islahbd
